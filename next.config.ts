import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "i.ibb.co",
      "i.imgur.com",
      "images.unsplash.com",
      "plus.unsplash.com",
      "cdn.pixabay.com",
    ],
  },
};

export default nextConfig;
